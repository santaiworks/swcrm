I will perform the re-engineering of the frontend and backend to use ID-based relational fields with Comboboxes.

### 1. Frontend: Update `MasterDataCombobox`
-   **Modify `frontend/components/leads/master-data-combobox.tsx`**:
    -   Update it to return the **ID** (Integer) instead of the Name in `onChange`.
    -   Update it to accept an **ID** as `value`.
    -   Implement logic to display the correct name for the selected ID (by finding it in the loaded items).

### 2. Frontend: Update `CreateLeadDialog` & `UpdateLeadDialog`
-   **Modify `frontend/components/leads/create-lead-dialog.tsx`**:
    -   Update state handlers to store IDs (numbers) instead of strings for master data fields.
-   **Modify `frontend/components/leads/update-lead-dialog.tsx`**:
    -   **Add missing fields**: Industry, Source, No Employees, Status (currently missing in the render).
    -   **Replace Input/Select**: Change `salutation` (currently hardcoded Select) and the new fields to use `MasterDataCombobox`.
    -   Ensure the form submits **IDs** to the backend.

### 3. Backend: Refactor Lead Model & Schema
-   **Modify `backend/app/modules/leads/models.py`**:
    -   Change `salutation`, `industry`, `source`, `no_employees`, `status` columns from `String` to `Integer` (Foreign Keys to master tables).
    -   (Optional) Add `ForeignKey` constraints for data integrity.
-   **Modify `backend/app/modules/leads/schemas.py`**:
    -   Update `LeadCreate` and `LeadUpdate` schemas to expect `int` (IDs) for these fields instead of `str`.

### 4. Verification
-   Ensure creating a new lead sends IDs.
-   Ensure updating a lead sends IDs.
-   Ensure the backend stores IDs correctly.
